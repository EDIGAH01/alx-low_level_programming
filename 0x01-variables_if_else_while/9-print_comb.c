#include<stdio.h>
#include<stdlib.h>
/**
 * main  - Entry point
 * printing 
 * Return: Always 0 (success)
 */
int main(void)
{
	int i;

	for  (i = 48; i < 58; i++)
	{
		putchar(n);
		if (i != 57)
		{
			putchar(",");
			putchar(" ");
		}
	}
	putchar('\n');

        return(0);
} 
